<h1>New Book</h1>

<%= form_with model: Book.new do |f| %>
<!--%= form_with model: @book,url: '/books',method: :post do |f| %>-->

 <h4>title</h4>
 <%= f.text_field :title %>

 <h4>body</h4>
 <%= f.text_area :body %>

 <%= f.submit 'Create Book' %>
 <!--showへのリンクを作るlink_to Create Book,book_path(book.id)-->
 <% end %>